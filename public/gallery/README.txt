Put any gallery photos here (.jpg, .jpeg, .png, .webp, .gif, .avif).

When you run `npm run dev` or `npm run build`, a manifest.json is generated listing every image in this folder — the site slideshow picks them up automatically (no manual list to edit).

You can still run only: npm run gallery-manifest
